apiVersion: v1
kind: Service
metadata:
    name: adjuneazuredevopsgithubacraksapp
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: adjuneazuredevopsgithubacraksapp